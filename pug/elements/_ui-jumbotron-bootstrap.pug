//- title - title of element
//- btnClass - button class
//- btnHref - button link
//- iconClass - class of icon inside button
//- iconAlign - icon aligment inside button
//- btnText - button text
//- desc - jumbotron description
mixin jumbotron-bootstrap(title, btnClass, btnHref, iconClass, iconAlign, btnText, desc, target)
  //Bootstrap jumbotron
  .jumbotron.shadow-drop-lg.context-dark.text-left
    h2
      span.big= title
    p= desc
    if iconClass != null
      a(href=btnHref class=btnClass + ' btn-icon-' + iconAlign target= target).btn.btn-icon.offset-top-14
        span.icon.novi-icon(class= iconClass)
        = btnText
    else
      a(href=btnHref class=btnClass target= target).btn.offset-top-14= btnText

