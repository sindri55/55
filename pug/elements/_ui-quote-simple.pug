//- quote - object of massive parameters

mixin quote-simple(quote)
  // Quote Simple
  hr(class= quote.firstDivider)
  div.h3.text-center.text-italic(class= quote.styling)
    q= quote.title
  hr(class= quote.lastDivider)
